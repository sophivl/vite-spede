{
  "name": "estudo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "live-server --baseDir=dist --browser=chrome",
    "start": "concurrently \"npm run watch\" \"npm run server\"",
    "watch": "tsc -w",
    "compile": "tsc"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bootstrap": "^5.3.3",
    "concurrently": "^9.0.1",
    "node": "^22.11.0",
    "shepherd.js": "^14.1.0",
    "vite": "^5.4.10"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
